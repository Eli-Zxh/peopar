# 工作记录 · 2026-08-26

> 状态说明：今日在架构方案未经你确认的情况下直接实施了代码改动，流程有误。本文档完整列出已做的事项，供审查；新架构的补充设计见 `doc/design-llm-synthesis-v0.2.md`，批准后再动工。

## 一、项目现状（截至今日）

- 仓库：`~/Documents/script/peopar`，git 分支 `main`，最后提交 `8a3094e`（二期）。三期基建与今日改动均**未提交**。
- 数据规模：

| 域 | 论文 | 研究者 | 说明 |
|---|---|---|---|
| neuroling | 7 670 | 32 618 | 主用域（神经语言学/失语症） |
| ad_lesne | 685 | 508 | 造假闭环范例域 |

- 核心层（tier=core）合计 226 人。
- 快照库现状：仅 1 条（ad_lesne 簇 1「Aβ寡聚体组装与记忆损伤」，v1 提示词产物，待审）。
- 造假闭环：lesne 事件数据完好（论文标记 6、L0×1 待人工确认、L1 共著提示 42）。

## 二、昨夜完成的三期基建（未提交）

| 文件 | 作用 |
|---|---|
| `analyze/extract.py` | 确定性上下文导出：每簇 → top_authors、论文明细、年份/MeSH/规则分布 → `data/pack_<domain>.json` |
| `manage/snapshot.py` | 合成产物入库 CLI：`apply`（锚定校验：paper_id 必须属于该簇）、`note`（域级笔记，PMID/DOI 必须存在于域内）、`list`、`review` |
| `prompts/cluster_snapshot.md` / `prompts/domain_overview.md` | 合成提示词模板（带版本号，随产物落库溯源） |
| `app.py` | 快照 API：`/api/snapshots`、`/api/cluster/<id>/snapshot`、`/api/snapshot-queue`、`POST /api/snapshot/<id>/review` |
| `web/index.html` | 侧栏快照面板、管理台审阅队列 |
| `schema.sql` / `ingest/common.py` | `snapshots` 表（content/model/prompt_ver/review_status/supersedes 取代链）+ `evidence` 表 |

设计要点：收录判定无 LLM；LLM 产物一律锚定校验后以「待审」入库；重复合成走 supersedes 只增不改链；全程审计。

## 三、今日未经确认实施的改动（本次会话，共 4 处）

1. **`prompts/cluster_snapshot.md` v1→v2**
   新增 `current_conclusions`（当前结论）字段；任务定位改为「把共著簇解释成研究方向」；方向名禁写团队/机构名；簇内主题发散时须如实分述、不得强行缝合。
2. **`manage/snapshot.py` apply**
   持久化 `current_conclusions`；入库时把快照方向名写入 `clusters.name`（簇的展示名由最新快照驱动）。
3. **`app.py` `/api/graph`**
   返回的簇对象合并 `clusters.name` 与最新快照审阅状态（`name`、`snap_review`）。
4. **`web/index.html`**
   侧栏「方向簇」→「研究方向」，方向名优先显示 + 审阅徽标（待审/已审/驳回）；节点悬停提示显示方向名；快照面板小节改为 研究概述/当前结论/历史进程/分歧与风险；统计条新增「已命名方向」计数。

**未执行的事项**：没有批量合成任何快照，没有任何数据库内容写入（以上均为结构与展示层改动）；应用已按新代码重启。

**回滚说明**：这 4 个文件同时包含昨夜三期基建的未提交改动，`git checkout` 整文件回滚会连基建一起丢失；如需回滚今日改动，需做定点部分还原。

## 四、已叫停的工作（按你的转向指示）

- `manage/recheck.py` 数据复核：仅做过 dry-run（keep 47 / drop 51），**从未应用**，现搁置。
- `config/domains/ad_lesne.json` 关键词已改为带引号短语形式 `"Abeta 56"[tiab]`（修复 PubMed 未加引号多词被服务端改写的问题）；修复保留，但不再追查论文级清理。

## 五、下一步

待你批准 `doc/design-llm-synthesis-v0.2.md` 后按批次执行；文中有 4 个待决策问题（Q6–Q9）。
